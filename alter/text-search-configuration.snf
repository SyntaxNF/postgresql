# https://www.postgresql.org/docs/18/sql-altertsconfig.html

# CASE ADD_MAPPING
ALTER TEXT SEARCH CONFIGURATION name
    ADD MAPPING FOR token [, ...]
    WITH dictionary [, ...]

# CASE ALTER_MAPPING
ALTER TEXT SEARCH CONFIGURATION name
    ALTER MAPPING FOR token [, ...]
    WITH dictionary [, ...]

# CASE REPLACE_DICTIONARY
ALTER TEXT SEARCH CONFIGURATION name
    ALTER MAPPING REPLACE dictionary
    WITH new_dictionary

# CASE REPLACE_MAPPING
ALTER TEXT SEARCH CONFIGURATION name
    ALTER MAPPING FOR token [, ...]
    REPLACE dictionary
    WITH new_dictionary

# CASE DROP_MAPPING
ALTER TEXT SEARCH CONFIGURATION name
    DROP MAPPING [ IF EXISTS ] FOR token [, ...]

# CASE RENAME
ALTER TEXT SEARCH CONFIGURATION name
    RENAME TO new_name

# CASE OWNER
ALTER TEXT SEARCH CONFIGURATION name
    OWNER TO role

# CASE SCHEMA
ALTER TEXT SEARCH CONFIGURATION name
    SET SCHEMA schema

# WHERE role
{ role_name | CURRENT_ROLE | CURRENT_USER | SESSION_USER }
